import { NewsletterBlock } from 'dramaeverafter-ds';

export const Canonical = () => (
  <div style={{ width: 320 }}>
    <NewsletterBlock
      title="The weekly drop"
      copy="One email a week: the new titles worth your coins, and which app has them."
      buttonLabel="Sign up"
    />
  </div>
);
