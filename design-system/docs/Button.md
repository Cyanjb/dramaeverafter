---
category: Actions
---

The gold/wine CTA button. Gold is the single primary action on a view (Search, Sign up); wine outline is secondary (Show 24 more). Renders as a link when `href` is given.

```tsx
<Button variant="gold" type="submit">Search</Button>
<Button variant="wine">Show 24 more</Button>
```
