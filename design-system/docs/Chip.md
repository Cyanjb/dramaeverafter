---
category: Actions
---

Trope/filter chip. Idle outline by default; `state="on"` wine fill when selected; `state="off"` greyed. Optional `count`.

```tsx
<Chip label="contract marriage" count="1,032" href="#" />
<Chip label="billionaire" state="on" />
```
